# heatyourimage
The Heatmap Image Converter is a Python application that allows users to turn any image into a colorful heatmap representation. The app processes the uploaded image, converts it to grayscale, resizes it to a manageable matrix, and generates a heatmap to visualize the intensity of each pixel. 
